# scoreboard

## 理論下限

### 1 ケースあたりの理論下限

- 最終長さを `k = M` にするには、少なくとも `M-5` 回の捕食が必要。
- 1 ターンで食べられる餌は高々 1 個なので、操作回数 `T` は少なくとも `M-5`。
- もし `k = M` かつ `E = 0` を達成できれば、絶対スコアは `T` だけになる。
- よって、各ケースの絶対スコアの理論下限は `M-5`。

### 生成範囲から見た下限の幅

- `M` は `ceil(N^2/4)` 以上 `floor(3N^2/4)` 以下。
- したがって理論下限 `M-5` は、全入力全体では `11` 以上 `187` 以下。
  - 最小: `N = 8`, `M = 16` のとき `M-5 = 11`
  - 最大: `N = 16`, `M = 192` のとき `M-5 = 187`

### 平均的な理論下限の目安

- 入力生成の平均でみると `E[M] ≒ 75.33`。
- したがって平均的な理論下限は `E[M-5] ≒ 70.33`。

## ランダム基準の見積もり

### 基準とするランダム解

- 「噛み切りなし」
- 「盤面をジグザグに全走査して全ての餌を食べる」
- このとき操作回数は常に `T = N^2 - 5`
- すべての餌を食べるので `k = M`

この基準は、移動は決め打ちだが、入力生成上は食べる色順が `d_5, ..., d_{M-1}` のランダム順列になる。

### 色不一致数の期待値

- 色ごとの個数を `m_c` とする。
- 固定された希望列に対して、ランダム順列を食べたときの一致数期待値は

```text
E[match] = (sum_c m_c^2) / (M-5)
```

- よって suffix の不一致数期待値は

```text
E[E_suffix] = (M-5) - (sum_c m_c^2) / (M-5)
```

- 初期 5 要素はすでに一致しているので、全体の不一致数期待値もこの値に等しい。

### バランスしているとみなした近似

- `m_c` がほぼ均等なら `sum_c m_c^2 / (M-5) ≒ (M-5)/C`
- このとき

```text
E[E] ≒ (M-5) * (1 - 1/C)
```

- `C = 3` なら不一致率は約 `2/3`
- `C = 5` なら不一致率は約 `4/5`
- `C = 7` なら不一致率は約 `6/7`

### 平均ケースの概算

- `E[N^2] ≒ 150.67`
- `E[M] ≒ 75.33`
- よって平均的なジグザグ全食べの操作回数は `E[T] ≒ 145.67`
- `C` を `3..7` 一様平均でみると `E[1 - 1/C] ≒ 0.7814`
- したがって、ランダム順ベースラインの不一致数は

```text
E[E] ≒ 54.96
```

- このとき絶対スコアはおおむね

```text
E[score] ≒ 145.67 + 10000 * 54.96 ≒ 549750
```

## スコア重みの読み方

- 不一致 `E` を 1 減らすと、絶対スコアは `10000` 改善する。
- 食べ残し `M-k` を 1 減らすと、絶対スコアは `20000` 改善する。
- したがって、少なくとも初期段階では `T` 短縮よりも `k = M` 達成と `E` 削減が重要。

## `M = k` 達成時の見え方

### スコア式の簡約

- `M = k` のとき、絶対スコアは

```text
score = T + 10000 * E
```

- つまり、この段階では `M-k` の重い罰は消え、勝負は `E` と `T` の 2 軸になる。

### 重みの差

- `E` を 1 減らす改善は `T` を `10000` 手縮める改善と同じ重みを持つ。
- したがって、`M = k` 到達後は `T` 短縮よりも `E` 削減を優先するのが自然。
- `M = k` に入った時点で、問題はほぼ「色順をどこまで正しくできるか」に移る。

### 目安

- `E = 0` なら、スコアはそのまま `T` になる。
- たとえば `T = 180`, `E = 40` なら

```text
score = 180 + 10000 * 40 = 400180
```

- たとえば `T = 500`, `E = 5` なら

```text
score = 500 + 10000 * 5 = 50500
```

- よって、多少遠回りでも `E` を数個減らせるなら十分に得になる。

### ランダム順に近い全食べとの比較

- 「全て食べたが色順はほぼランダム」という状態でも、`M = k` ならスコアはおおむね `T + 10000 * E` に収まる。
- 色がほぼ均等とみなせるとき、各 suffix 位置が一致する確率はおおむね `1/C` とみなせる。
- この近似では、不一致数期待値は

```text
E[E | M = k] ≒ (M-5)(1 - 1/C)
```

- よって、平均入力に対しては

```text
E[M-5] ≒ 70.33
E[1 - 1/C] ≒ 0.78143
E[E | M = k] ≒ 70.33 * 0.78143 ≒ 54.96
```

- したがって、`M = k` だが `E != 0` のランダム順ベースでは、色不一致ペナルティだけでおおむね

```text
10000 * E[E | M = k] ≒ 549600
```

- になる。
- 既存の平均ケース概算では、この状態の不一致数期待値は `E[E] ≒ 54.96`。
- したがって、`M = k` だが順序が悪い解の平均的なスコアはおおむね `50` 万点台になる。
- ここからの本質的な改善は、`T` よりもまず `E` をどこまで下げられるかで決まる。

## 現在値

- 現在のローカルベスト: `6,549,798`（2026-04-11 時点）
- 現在の提出スコア: `6,031,751`（2026-04-11 時点）
- 順位: `263`
- 順位表上の相対スコア合計: `35,855,612,247`
- 提出スコアとローカルの差: `-518,047`（提出のほうが `7.91%` 良い）
- 直近の 200 ケース換算での `completed`: `111 / 200`
- 直近の 200 ケース換算での `full_length`: `160 / 200`
- 直近の 200 ケース換算での未完了ケース平均 `E`: `19.98`

## スコア履歴

| 日付 | local | submit | 順位 | 概要 |
| --- | ---: | ---: | ---: | --- |
| 2026-04-11 | 6,549,798 | 6,031,751 | 263 | 食べ直し評価の導入で completed をさらに増やし、safe_branch_full_length を減らしたが、提出スコア自体は据え置き |
| 2026-04-10 | 7,304,131 | 6,031,751 | 256 | bite 前 snapshot 保存と greedy 評価整理まで反映し、main 側の良い状態保全と future-aware greedy をさらに強化 |
| 2026-04-09 | 8,036,597 | 8,433,863 | 270 | GreedyTarget の 3 手先連続一致評価を追加し、future-aware な no-bite 継続をさらに強化 |
| 2026-04-09 | 10,633,465 | 11,585,517 | 298 | snapshot 保存条件を `M = k`、`prefix_len` 更新時、`no_plan` 直前まで広げ、main 側の良い状態を保険として拾う方針を反映 |
| 2026-04-09 | 12,176,586 | 13,406,604 | 304 | `BiteRebuild` に last resort 候補を追加し、`no_plan` を減らして completed を増やす方針を反映 |
| 2026-04-08 | 11,640,601 | 12,973,037 | 274 | SafeCollect の bite 候補評価、回収経路、bite -> zigzag、発火条件の改善を段階的に反映 |
| 2026-04-08 | 13,904,527 | 15,033,134 | 291 | GreedyFallback にも複数候補比較を入れ、target/fallback の両方で候補を 1 本固定しない方針を反映 |
| 2026-04-08 | 14,671,109 | 16,083,769 | 305 | simulator を release 実行へ切り替え、複数経路候補の検討に向けた整理を反映 |
| 2026-04-07 | 16,165,150 | 13,821,790 | 266 | BFS 可視化と動的到達可能性の検証に向けたビジュアライザ整備を反映 |
| 2026-04-05 | 14,573,199 | 15,323,747 | 155 | safe branch による保険解保存を入れ、本線と保険解の良い方を採用 |
| 2026-04-04 | 26,795,627 | 27,546,027 | 158 | 脱出用 bite 後に greedy へ戻す遷移と、終盤の forced SafeCollect 条件を反映 |
| 2026-04-04 | 21,182,416 | 22,202,937 | 147 | SafeCollect に deep bite を入れ、`M = k` 達成を優先する方針を反映 |
| 2026-04-04 | 47,190,170 | 46,463,107 | 238 | BFS 到達判定、色ターゲット選択、経路復元による greedy 初版を反映 |
| 2026-04-04 | 24,044,670 | 24,447,131 | 109 | 初期 ToDo 一式実施後。local は `simulator.py` 200ケース実行結果の50ケース換算 |

## 現時点の見立て

- 今回の local は前回 `7,304,131` から `754,333` 改善し、改善率は約 `10.33%` だった。
- 一方で提出スコアは `6,031,751` のまま据え置きで、順位は `256` から `263` へ下がった。local で効いた改善が提出 50 ケースにはまだ十分乗っていない可能性がある。
- 提出はローカルより `518,047` 良く、差分は約 `7.91%` だった。以前より提出と local のギャップは縮まっている。
- 1 ケース平均に直すと、local は約 `130,996`、submit は約 `120,635` で、どちらもランダム順全食べ基準の約 `549,750` を大きく下回っている。
- 直近の 200 ケース集計では `completed = 111 / 200`、`full_length = 160 / 200` まで来ており、completed の増加と `safe_branch_full_length` の減少が主改善要因になっている。
- ただし `full_length` は `160 / 200` まで下がっており、`pre_bite_snapshot` による非 full-length の良い中間解を多く拾う方向がかなり強く効いている。
- 次は `pre_bite_snapshot -> completed` と `main_full_length_snapshot -> completed` を押し上げつつ、full-length をどこまで維持するかのバランス調整が重要である。

## 相対スコアの見立て

### 現在位置

- 順位表上の相対スコア合計は `35,855,612,247`。
- `200位` の相対スコアは `38,601,020,411`。
- `100位` の相対スコアは `43,461,734,694`。
- `10位` の相対スコアは `49,039,795,918`。
- `1位` の相対スコアは `49,653,571,430`。

### 目標順位までの距離

- `200位` までの差は `2,745,408,164`。
- `100位` までの差は `7,606,122,447`。
- `1位` までの差は `13,797,959,183`。

### 解釈

- この問題の相対スコアは各ケース `10^9` 点満点の順位スコア合計なので、上位ほど多くのケースで高順位を安定して取っていることを意味する。
- `1位〜10位` が `49B` 台に集まっていることから、最上位陣は多くのケースで `M = k, E = 0` にかなり近く、残りは主に `T` を数手〜数十手単位で詰めている可能性が高い。
- `100位〜200位` 帯も、かなりの割合で `M = k, E = 0` か、それに非常に近い状態まで到達していると考えるのが自然である。
- 現在の `256位` は、`completed` をさらに増やしつつ `main_full_length_snapshot` / `safe_branch_full_length` を減らせれば、まず `200位以内` は十分射程に入る位置と見てよい。

## 200 ケース結果の見立て

- 200 ケース実行では、`completed = 111 / 200` に達しており、完全達成率は `55.5%` まで上がっている。
- これは、現在の方針で半数超の seed を end-to-end に解けていることを意味する。
- 一方で未達 `89 / 200` の中には、`pre_bite_snapshot` や `main_full_length_snapshot` のような still-good な中間解が多く含まれている。
- したがって、当面の主戦場は「完全に失敗する seed を救う」から、「良い中間解を completed に押し上げる」方向へ移ってきている。
- 上位勢は多くのケースで `M = k, E = 0` に近い状態まで持ち込み、そこから `T` を削っている可能性が高い。
- こちらも引き続き `completed` を増やすことが本筋だが、同時に `completed` 群の `T` 改善もそろそろ意味を持ち始めている。

## 最新 run の停止理由別集計

最新の 200 ケース run では、`stop_reason` ごとの件数と平均値は次の通りだった。

| stop_reason | 件数 | 平均 score | 平均 E | 平均 T | 平均 elapsed(ms) | 平均 M |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `completed` | 111 | 861.84 | 0.00 | 861.84 | 728.12 | 47.54 |
| `pre_bite_snapshot` | 39 | 303683.03 | 9.38 | 2657.38 | 1929.97 | 109.92 |
| `main_full_length_snapshot` | 33 | 74125.73 | 7.15 | 2610.58 | 1814.67 | 78.94 |
| `safe_branch_full_length` | 16 | 737072.50 | 73.50 | 2072.50 | 1930.38 | 146.25 |
| `main_prefix_snapshot` | 1 | 20582.00 | 0.00 | 582.00 | 1935.00 | 81.00 |

### 停止理由ごとの見立て

- `completed`
  - 件数は `111` まで増えており、今回も改善の主成果である。
  - 平均 `M = 47.54` なので、少し大きめのケースまで completed に押し上げられている。
  - 今後は件数増だけでなく、`T` の削減も狙える段階に近づいている。

- `pre_bite_snapshot`
  - 件数は `39` と多く、今回の run では大きな受け皿になっている。
  - 平均 `E = 9.38` と still-good だが、`M = k` を捨てて bite 前の良い状態を採る方向がかなり強い。
  - ここをそのまま良しとするか、completed に押し上げるかが次の重要論点である。

- `safe_branch_full_length`
  - 件数は `21 -> 16` とさらに減った。
  - ただし平均 `E = 73.50`、平均 `M = 146.25` と依然かなり重く、残るケースは難ケース寄りと見てよい。
  - 件数は少なくなったが、1 件あたりの重さはまだ非常に大きい。

- `main_full_length_snapshot`
  - 件数は `34 -> 33` と微減。
  - 平均 `E = 7.15` とかなり良く、ここをさらに completed に寄せる余地は大きい。
  - 代表 seed を見て、何が足りずに completed へ届かないかを整理する価値が高い。

- `main_prefix_snapshot`
  - 件数は `1` まで減っており、問題はかなり局所化した。
  - 平均 `E = 0` なので prefix は非常に良いが、長さ不足を許してしまっている。
  - 件数は少なく切り分けしやすいので、代表 seed を visualizer で見て「残り餌をなぜ取り切れないか」を確認する価値が高い。

### 次に対する対応

- 最優先
  - `pre_bite_snapshot` と `main_full_length_snapshot` を completed に寄せる改善。
  - どちらも still-good な中間解なので、ここを押し上げる改善幅が最も大きい。

- 次点
  - `main_prefix_snapshot` の原因切り分けと full-length 崩れの抑制。
  - 件数は `1` まで減ったが、確実に潰せるならそのまま `full_length` 改善につながる。

- 後回し
  - `safe_branch_full_length` の質をさらに上げる改善。
  - 件数はかなり減ったので優先度は少し下がったが、残る群は非常に重いため継続監視は必要である。
