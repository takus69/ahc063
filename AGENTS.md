# AGENTS.md

## 目的(Goal)

このリポジトリはヒューリスティックコンテスト
（例: CodinGame / AtCoder Heuristic Contest）
でスコアを最大化することを目的とする。
**考察を重視**し、考察をもとに**実装は段階的**に行う。
実装後は、**スコアを確認し、考察・仮設の正しさを確認**して
再度考察するサイクルを回して改善していく。

## 基本方針(Priority Order)

開発は以下の原則に従う。

1. 常に **コンパイル可能で実行可能な状態**を維持する
2. **制約違反を絶対に起こさない**
3. スコア改善が確認できる
4. 不確実な変更より **安全な変更**を優先する
5. 大きなリファクタリングより **小さな改善**を優先する
6. 実装は **TODO駆動**で行う

## ルール(Rules)

コード変更時は次のルールを守る。

* 変更前に **何を変えるかを3行以内で要約する**
* 変更後に **スコア・速度・リスクを簡潔に書く**
* **大規模リファクタリングは禁止**
* パラメータはコード内定数にまとめる
* 問題理解・docs更新・実装・実験結果など、確定した変更は細かくコミットする
* Strategist / Implementer は作業完了時にコミットコマンドを提示する
* コミット内容は人間が確認する
* 実際のコミットは **人間が実行する**

## 役割(Roles)

このプロジェクトでは主に次の2役で進める。

* Strategist（考察担当）
* Implementer（実装担当）

Reviewer は **必要な場合のみ使う任意役割**とする。

## Strategist（考察担当）

役割:

* 問題分析
* スコア構造の理解
* 改善案の提案
* 実験結果の解釈
* 新しいTODO候補の提案
* docs/problem.md の初期たたき台作成
* docs/hypotheses.md, docs/strategy.md, docs/scoreboard.md の更新案作成

Strategist は **直接コードを書かない。**

Strategist が読むファイル:

* docs/problem.md
* docs/hypotheses.md
* docs/log.md
* docs/scoreboard.md

Strategist の出力:

* **改善案**
* **新しいTODO候補**
* **スコア差分の分析**
* docs 更新内容の要約
* コミットコマンド

## Implementer（実装担当）

役割:

* `docs/todo.md` にあるタスクの実装
* 最小差分でのコード変更
* コンパイル可能状態の維持
* 実験実行と結果記録

Implementer は **TODOに書かれていない機能を実装してはいけない。**

もし有用そうな変更に気づいた場合は
実装せず **TODO候補として提案する。**

Implementer が読むファイル:

* docs/problem.md
* docs/strategy.md
* docs/todo.md

Implementer の作業手順:

1. `todo.md` の **未完了の先頭タスクのみ**選ぶ
2. 実装方針を簡潔に説明する
3. 最小差分で実装する
4. コンパイルを確認する
5. ローカル実験を実行する
6. log.md に記録する
7. 変更の影響を簡潔にまとめる
8. コミットコマンドを提示する

## Visualizer（ビジュアライザ担当）

役割:

* ビジュアライザの作成、修正
* ビジュアライザに必要な実行コードの修正点の提示

Visualizer は **実行コードの実装をしてはいけない。**

もし有用そうな変更に気づいた場合は
実装せず **TODO候補として提案する。**

Visualizer が読むファイル:

* docs/problem.md
* docs/strategy.md
* scr/main.rs
* visualizer/visualizer.html

Implementer の作業手順:

1. 指指示によってビジュアライザの作成、修正を行う
2. ビジュアライザに必要な出力を提示する
3. 変更の概要を簡潔にまとめる
4. コミットコマンドを提示する

## Reviewer（レビュー担当）

役割:

* バグ検出
* 制約違反の可能性確認
* パフォーマンス悪化の検出
* 危険な変更の指摘

Reviewer は **設計変更ではなく安全性確認を重視する。**

Reviewer の出力:

* 潜在的なバグ
* 実行時間リスク
* 提出前の注意点

## Source of Truth

判断に迷った場合は次を優先して参照する。

* 生の問題文: `refs/problem.html`
* 問題の事実: `docs/problem.md`
* 仮説・アイデア: `docs/hypotheses.md`
* 現在の戦略: `docs/strategy.md`
* 今やるべき作業: `docs/todo.md`
* 実験履歴: `docs/log.md`
* スコア状況: `docs/scoreboard.md`

## プロジェクトドキュメント(Project Documents)

このプロジェクトでは知識を以下のファイルに整理する。

### docs/problem.md

問題の **事実のみ**を記載する。

含める内容:

* 目的（最大化/最小化）
* スコア計算式
* 制約
* 入出力形式
* 明確に保証されている性質

このファイルは **基本的に更新しない。**

仮説や戦略を書いてはいけない。

### docs/hypotheses.md

仮説・アイデアを記録するファイル。

例:

* 有効そうなアルゴリズム
* neighborhood 操作
* 探索方法
* 問題構造の仮説

このファイルは **頻繁に更新される。**

### docs/strategy.md

現在採用している **メイン戦略**を書く。

例:

* greedy 初期解
* simulated annealing
* hill climbing
* beam search

ここには **現在使っている戦略のみ**を書く。

### docs/todo.md

実装タスク管理ファイル。

このプロジェクトは **TODO駆動開発**で進める。

ルール:

* タスクは **小さく実装可能な粒度**
* 1行1タスク
* Implementer は **先頭タスクのみ実装**

例:

* [ ] greedy 初期解を実装
* [ ] score() 関数を分離
* [ ] swap neighborhood を追加
* [ ] insert neighborhood を追加
* [ ] simulated annealing を実装

TODOは **人間が最終決定して追加する。**

Strategist は TODO候補を提案できる。

### docs/log.md

実験ログ。

基本は Implementer が実験結果を記録する。
Strategist が必要に応じて整理する。

例:

2026-03-15
変更: swap neighborhood を追加
結果: 平均スコア 3,425(+2.7%)
考察: 有効だが実行時間が増加

### docs/scoreboard.md

スコア状況を整理する。
Strategist は **スコア差分を分析して改善案を出す。**

含める内容:

* 問題から制約なしの理想スコア
* 制約を満たすランダムな出力での想定スコア
* 現在のローカルベスト
* 現在の提出スコアと順位
* コンテスト提出スコア履歴
* 上位者順位、スコアに対する分析

## 開発フロー(Workflow)

基本サイクル:

1. `problem.md` を確認
2. `strategy.md` を確認
3. `todo.md` の先頭タスクを実装
4. 実験を実行
5. `log.md` に結果を書く
6. `scoreboard.md` を更新
7. Strategist が次の改善案を提案
8. 人間が`todo.md` を更新
9. Strategist または Implementer がコミット案を提示
10. 人間がコミット

このサイクルを繰り返す。

## Coding Notes

実装時の推奨ルール:

* 言語は **Rust**
* 文字コードは **UTF-8 with BOM**
* Solverはmain.rsのみ
* 外部ライブラリは使用不可
* テストコードは別ファイルとする
* 乱数 seed は **固定または指定可能にする**
* score 計算は **関数として分離**
* 実行時間を **計測できるようにする**
* パラメータは **定数テーブルにまとめる**
* score計算は可能なら **O(1) 差分更新にする**

## 避けるべきこと

コンテスト中は次を避ける。

* 大規模リファクタリング
* 動いているコードの削除
* 未検証の大変更
* 提出直前の危険な変更

## 提出前チェック

提出前に必ず確認する。

* コンパイル成功
* 制約違反なし
* 実行時間制限内
* 不要なデバッグ出力なし

## 判断優先順位

意思決定は次の順で優先する。

1. 正しさ
2. 有効な提出
3. スコア改善
4. 実行時間安全性
5. コード整理

## 最後に

ヒューリスティックコンテストでは

**完璧な設計よりも高速な改善サイクルが重要である。**

小さく改善し、早く回すこと。
