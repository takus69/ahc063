# scoreboard

## 理論下限

### 1 ケースあたりの理論下限

- 最終長さを `k = M` にするには、少なくとも `M-5` 回の捕食が必要。
- 1 ターンで食べられる餌は高々 1 個なので、操作回数 `T` は少なくとも `M-5`。
- もし `k = M` かつ `E = 0` を達成できれば、絶対スコアは `T` だけになる。
- よって、各ケースの絶対スコアの理論下限は `M-5`。

### 生成範囲から見た下限の幅

- `M` は `ceil(N^2/4)` 以上 `floor(3N^2/4)` 以下。
- したがって理論下限 `M-5` は、全入力全体では `11` 以上 `187` 以下。
  - 最小: `N = 8`, `M = 16` のとき `M-5 = 11`
  - 最大: `N = 16`, `M = 192` のとき `M-5 = 187`

### 平均的な理論下限の目安

- 入力生成の平均でみると `E[M] ≒ 75.33`。
- したがって平均的な理論下限は `E[M-5] ≒ 70.33`。

## ランダム基準の見積もり

### 基準とするランダム解

- 「噛み切りなし」
- 「盤面をジグザグに全走査して全ての餌を食べる」
- このとき操作回数は常に `T = N^2 - 5`
- すべての餌を食べるので `k = M`

この基準は、移動は決め打ちだが、入力生成上は食べる色順が `d_5, ..., d_{M-1}` のランダム順列になる。

### 色不一致数の期待値

- 色ごとの個数を `m_c` とする。
- 固定された希望列に対して、ランダム順列を食べたときの一致数期待値は

```text
E[match] = (sum_c m_c^2) / (M-5)
```

- よって suffix の不一致数期待値は

```text
E[E_suffix] = (M-5) - (sum_c m_c^2) / (M-5)
```

- 初期 5 要素はすでに一致しているので、全体の不一致数期待値もこの値に等しい。

### バランスしているとみなした近似

- `m_c` がほぼ均等なら `sum_c m_c^2 / (M-5) ≒ (M-5)/C`
- このとき

```text
E[E] ≒ (M-5) * (1 - 1/C)
```

- `C = 3` なら不一致率は約 `2/3`
- `C = 5` なら不一致率は約 `4/5`
- `C = 7` なら不一致率は約 `6/7`

### 平均ケースの概算

- `E[N^2] ≒ 150.67`
- `E[M] ≒ 75.33`
- よって平均的なジグザグ全食べの操作回数は `E[T] ≒ 145.67`
- `C` を `3..7` 一様平均でみると `E[1 - 1/C] ≒ 0.7814`
- したがって、ランダム順ベースラインの不一致数は

```text
E[E] ≒ 54.96
```

- このとき絶対スコアはおおむね

```text
E[score] ≒ 145.67 + 10000 * 54.96 ≒ 549750
```

## スコア重みの読み方

- 不一致 `E` を 1 減らすと、絶対スコアは `10000` 改善する。
- 食べ残し `M-k` を 1 減らすと、絶対スコアは `20000` 改善する。
- したがって、少なくとも初期段階では `T` 短縮よりも `k = M` 達成と `E` 削減が重要。

## `M = k` 達成時の見え方

### スコア式の簡約

- `M = k` のとき、絶対スコアは

```text
score = T + 10000 * E
```

- つまり、この段階では `M-k` の重い罰は消え、勝負は `E` と `T` の 2 軸になる。

### 重みの差

- `E` を 1 減らす改善は `T` を `10000` 手縮める改善と同じ重みを持つ。
- したがって、`M = k` 到達後は `T` 短縮よりも `E` 削減を優先するのが自然。
- `M = k` に入った時点で、問題はほぼ「色順をどこまで正しくできるか」に移る。

### 目安

- `E = 0` なら、スコアはそのまま `T` になる。
- たとえば `T = 180`, `E = 40` なら

```text
score = 180 + 10000 * 40 = 400180
```

- たとえば `T = 500`, `E = 5` なら

```text
score = 500 + 10000 * 5 = 50500
```

- よって、多少遠回りでも `E` を数個減らせるなら十分に得になる。

### ランダム順に近い全食べとの比較

- 「全て食べたが色順はほぼランダム」という状態でも、`M = k` ならスコアはおおむね `T + 10000 * E` に収まる。
- 色がほぼ均等とみなせるとき、各 suffix 位置が一致する確率はおおむね `1/C` とみなせる。
- この近似では、不一致数期待値は

```text
E[E | M = k] ≒ (M-5)(1 - 1/C)
```

- よって、平均入力に対しては

```text
E[M-5] ≒ 70.33
E[1 - 1/C] ≒ 0.78143
E[E | M = k] ≒ 70.33 * 0.78143 ≒ 54.96
```

- したがって、`M = k` だが `E != 0` のランダム順ベースでは、色不一致ペナルティだけでおおむね

```text
10000 * E[E | M = k] ≒ 549600
```

- になる。
- 既存の平均ケース概算では、この状態の不一致数期待値は `E[E] ≒ 54.96`。
- したがって、`M = k` だが順序が悪い解の平均的なスコアはおおむね `50` 万点台になる。
- ここからの本質的な改善は、`T` よりもまず `E` をどこまで下げられるかで決まる。

## 現在値

- 現在のローカルベスト: `13,904,527`（2026-04-08 時点）
- 現在の提出スコア: `15,033,134`（2026-04-08 時点）
- 順位: `291`
- 提出スコアとローカルの差: `+1,128,607`（提出のほうが `8.12%` 悪い）
- 200 ケース換算での `completed`: `59 / 200`
- 200 ケース換算での `full_length`: `200 / 200`
- 200 ケース換算での未完了ケース平均 `E`: `39.06`

## スコア履歴

| 日付 | local | submit | 順位 | 概要 |
| --- | ---: | ---: | ---: | --- |
| 2026-04-08 | 13,904,527 | 15,033,134 | 291 | GreedyFallback にも複数候補比較を入れ、target/fallback の両方で候補を 1 本固定しない方針を反映 |
| 2026-04-08 | 14,671,109 | 16,083,769 | 305 | simulator を release 実行へ切り替え、複数経路候補の検討に向けた整理を反映 |
| 2026-04-07 | 16,165,150 | 13,821,790 | 266 | BFS 可視化と動的到達可能性の検証に向けたビジュアライザ整備を反映 |
| 2026-04-05 | 14,573,199 | 15,323,747 | 155 | safe branch による保険解保存を入れ、本線と保険解の良い方を採用 |
| 2026-04-04 | 26,795,627 | 27,546,027 | 158 | 脱出用 bite 後に greedy へ戻す遷移と、終盤の forced SafeCollect 条件を反映 |
| 2026-04-04 | 21,182,416 | 22,202,937 | 147 | SafeCollect に deep bite を入れ、`M = k` 達成を優先する方針を反映 |
| 2026-04-04 | 47,190,170 | 46,463,107 | 238 | BFS 到達判定、色ターゲット選択、経路復元による greedy 初版を反映 |
| 2026-04-04 | 24,044,670 | 24,447,131 | 109 | 初期 ToDo 一式実施後。local は `simulator.py` 200ケース実行結果の50ケース換算 |

## 現時点の見立て

- 今回の local は前回 `14,143,928` から `239,401` 改善し、改善率は約 `1.69%` だった。
- 提出も前回 `16,083,769` から `1,050,635` 改善し、順位も `305` から `291` へ改善した。
- したがって、GreedyFallback の複数候補比較は小幅ながら正方向の改善と見てよい。
- 最新の 200 ケース集計では `completed = 59 / 200`、`full_length = 200 / 200` で、未達ケースの平均 `E` は `39.06` だった。
- つまり `M = k` は引き続き全ケースで達成できており、主戦場はやはり `E` の削減にある。
- 1 ケース平均に直すと、local は約 `278,091`、submit は約 `300,663` で、どちらもランダム順全食べ基準の約 `549,750` を大きく下回っている。
- 提出のほうがローカルより `1,128,607` 悪く、評価ぶれや seed 差は依然として残っている。
- completed が増えている一方で、1 回の再計画にかける計算量も増えている可能性があり、今後は候補数を増やしすぎずに評価関数の質を上げる方向がよさそうである。
- 提出実行時間は `1003ms` でまだ制限内だが、以前よりかなり増えているため、今後の改善では時間増加を監視する必要がある。
- 次に見るべきは、未達ケースで `E` がどこまで減っているかと、どの理由で solver が停止しているかである。

## 200 ケース結果の見立て

- 200 ケース実行では、`score <= 10000` の seed が `32 / 200` あり、完全達成率は `16.0%` だった。
- `score <= 10000` は実質的に `M = k` かつ `E = 0` を達成したケースとみなしてよい。
- この結果は、現在の方針で end-to-end に解ける seed がすでに一定数あることを示している。
- 一方で全体スコアを支配しているのは、未達の `168 / 200` ケースである。
- したがって、当面の主戦場は「良い seed をさらに少し削る」よりも、「失敗 seed を達成 seed に変える」ことにある。
- 上位勢は多くのケースで `M = k, E = 0` に近い状態まで持ち込み、そこから `T` を削っている可能性が高い。
- こちらもまずは完全達成率を上げ、その後に `T` 短縮へ移る方針が自然である。

## 最新 run の停止理由別集計

最新の 200 ケース run では、`stop_reason` ごとの件数と平均値は次の通りだった。

| stop_reason | 件数 | 平均 score | 平均 E | 平均 T | 平均 elapsed(ms) | 平均 M |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `completed` | 55 | 1048.13 | 0.00 | 1048.13 | 864.58 | 38.18 |
| `no_plan` | 45 | 65169.44 | 6.33 | 1836.11 | 1016.13 | 47.91 |
| `safe_branch_full_length` | 96 | 540367.56 | 53.70 | 3388.40 | 1895.92 | 102.95 |
| `force_safe_full_length` | 4 | 654919.00 | 64.75 | 7419.00 | 1967.75 | 112.75 |

### 停止理由ごとの見立て

- `completed`
  - `M` が小さめのケースで多く、平均 `M = 38.18`。
  - 平均 `E = 0` で、現在の solver が小さいケースでは end-to-end に解けていることを示している。
  - この群はすでに十分良いので、優先度は低い。

- `no_plan`
  - 平均 `M = 47.91` と中くらいのケースで起きやすい。
  - 平均 `E = 6.33` しか残っておらず、スコアも `65169.44` と比較的良い。
  - 「あと少しで完成」寄りの停止なので、後回しでもよいが、将来的には Greedy や BiteRebuild の微改善で completed に寄せられる可能性が高い。

- `safe_branch_full_length`
  - 件数が `96` と最も多く、平均 `M = 102.95` と大きいケースで支配的に発生している。
  - 平均 `E = 53.70` と大きく、平均 score も `540367.56` と高い。
  - つまり、現在の平均スコアを最も悪化させているのは「大きい `M` ケースで safe branch に落ち、`M = k` までは作るが色順が大きく崩れる」パターンである。
  - 次に最も優先して改善すべき対象である。

- `force_safe_full_length`
  - 件数は `4` と少ないが、平均 `M = 112.75`、平均 `E = 64.75` でかなり重い。
  - まずは `safe_branch_full_length` を改善したあとで触るべき対象である。

### 次に対する対応

- 最優先
  - SafeCollect / safe branch の品質改善。
  - 特に safe 中の bite 候補評価と、`bite -> zigzag` の質を上げて、大きい `M` ケースの `E` を下げたい。

- 次点
  - `stop_reason = no_plan` のケースを completed に寄せる小改善。
  - 平均 `E = 6.33` なので、Greedy や BiteRebuild の軽い改善で完成に届く余地がある。

- 後回し
  - `force_safe_full_length` の細かい調整。
  - 件数が少ないので、先に `safe_branch_full_length` を触る方が効率が良い。
